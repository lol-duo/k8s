apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: efs-claim-monitoring-prometheus
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: 5Gi